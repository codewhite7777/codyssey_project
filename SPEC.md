# Codyssey 올인원 과정 — 통합 작업 명세 (SPEC)

> 이 문서는 **과제를 어떻게 진행하고 관리할 것인가**에 대한 운영 규칙이다.
> 개별 미션의 요구사항은 `missions/<미션ID>/MISSION_SPEC.md`에 따로 쓴다.
> 규칙이 현실과 충돌하면 규칙을 고친다. 단, 고칠 때는 이 문서를 먼저 고친다.

---

## 0. 이 명세가 존재하는 이유

`references/eval-criteria.md`에 정리한 대로, Codyssey 평가는 **동작하는 코드만으로 통과하지 않는다.**

- 평가문항 **항목 1**만 "동작하는가?"를 묻는다.
- **항목 2~4**는 전부 "**설명할 수 있는가?**"를 묻는다. (구술)
- 수집된 타인 평가 샘플 2건은 모두 결과 `FAIL` / 경험정도 `재구현 불가`였다.

따라서 이 워크스페이스의 제1 원칙은 다음과 같다.

> **설명할 수 없는 코드는 미완성이다.**
> 코드가 돌아가도, 그 코드가 *무슨 작업을 하는지*와 *왜 그렇게 썼는지*가
> 주석과 노트에 남아 있지 않으면 해당 요구사항은 `DONE`이 아니다.

이 원칙이 아래 모든 규약(특히 §4 주석 규약)의 근거다.

---

## 1. 디렉터리 구조

```
codyssey_project/                 # 관리 모노레포 → github.com/codewhite7777/codyssey_project
├─ SPEC.md                        # ← 이 문서. 운영 규칙
├─ INDEX.md                       # 전 미션 진도 보드 (단일 진실 원천)
├─ templates/                     # 미션 착수 시 복사해 쓰는 원본
│  ├─ MISSION_SPEC.md             #   과제 분석 → 구현 명세
│  ├─ NOTE.md                     #   미션별 단일 노트 (개념 + 코드해설 + 구술 Q&A)
│  ├─ CHECKLIST.md                #   평가문항 역매핑 검증 체크리스트
│  └─ ISSUE_REPORT.md             #   장애 분석 리포트 (GitHub Issue 형식)
├─ references/
│  └─ eval-criteria.md            # 평가 채점 패턴 역설계 (타인 샘플 기반)
└─ missions/
   └─ <단계슬러그>/                # 1·2·3단계. 예: stage-1-ai-sw-basic
      ├─ README.md                #   단계 개요 + 소속 과제 목록
      └─ <번호>-<구분>-<슬러그>/  #   예: b1-1-web-portfolio
         ├─ MISSION_SPEC.md       # 이 과제의 요구사항·범위·완료 정의
         ├─ NOTE.md               # 이 과제의 개념/학습/구술 대비 (단일 노트)
         ├─ CHECKLIST.md          # 제출 전 자가 검증
         ├─ source/               # 과제 원문 — 수정 금지
         ├─ evidence/             # 검증 증거 (스크린샷, 로그, 명령어 출력)
         └─ repo/                 # 제출용 GitHub repo (submodule)
```

### 3단계 구성

과정은 **3개 학습단계**로 이루어지고, **한 단계 안에 과제가 여러 개** 들어간다.
과제 위에는 `구분`(학습주제)이 한 겹 더 있지만, 폴더를 3중으로 파지 않고
`MISSION_SPEC.md`의 메타데이터와 `INDEX.md`의 열로 관리한다. (탐색 비용 < 분류 이득)

| 단계 | 슬러그 |
|---|---|
| 1단계 · AI/SW 기초 | `stage-1-ai-sw-basic` |
| 2단계 · AI/SW 심화 | `stage-2-ai-sw-advanced` |
| 3단계 · AI/SW 응용 | `stage-3-ai-sw-applied` |

```
단계(stage)        →  구분(분야)            →  과제(mission)
1단계 AI/SW 기초    →  웹 기초와 프론트엔드   →  나를 소개하는 웹페이지 만들기 (B1-1)
                   →  Linux와 OS            →  …
```

### 과제 폴더 명명 규칙 — `<번호>-<구분접두사>-<과제슬러그>`

```
b1-1-web-portfolio
└┬─┘ └┬┘ └───┬───┘
 │    │      └─ 과제 슬러그 : 불변. 내용 기반 식별자
 │    └──────── 구분 접두사 : web / linux / python-git / ds / db / cloud
 └───────────── 과제 번호   : 가변. 개편 시 폴더명까지 바뀐다
```

번호를 폴더명에 넣는 대가로 **개편 시 rename 1회**를 지불한다. 대신 터미널·탐색기에서
번호로 즉시 찾히고, 이름순 정렬이 곧 진행 순서가 된다. 과제 수가 수십 개로 늘어날수록
이 이득이 rename 비용보다 크다.

**번호가 바뀌면 고칠 곳 4군데** (이보다 늘어나면 규칙이 잘못된 것이다):

1. `git mv missions/<단계>/<옛이름> missions/<단계>/<새이름>` — `git mv`는 이력을 보존한다
2. `MISSION_SPEC.md` 머리표의 과제번호 행
3. `INDEX.md`의 해당 행
4. 단계 `README.md`의 해당 행

문서 제목과 커밋 스코프도 폴더명을 그대로 쓴다.

```
문서 제목   : B1-1 web-portfolio — 구현 명세
커밋 스코프 : feat(b1-1-web-portfolio): 다크 모드 토글과 로컬스토리지 유지 구현
```

`eval_pdf/`의 평가 자료는 **번호가 바뀌기 전 자료**다. 번호·미션명 대조에 쓰지 말고
`references/eval-criteria.md`가 정리한 **채점 구조** 참고용으로만 쓴다.

### 저장소 규칙

- **모노레포**(`codyssey_project`)는 관리·학습 산출물의 홈이다. 여기에 개념과 진도가 축적된다.
  추적 대상은 **명세·노트·체크리스트·증거**뿐이다. 제출 코드는 추적하지 않는다.
- **제출용 코드**는 과제별 독립 GitHub repo가 단독 소유한다.
  로컬에서는 `missions/<단계>/<과제>/repo/`에 clone해 두고, 모노레포는 `.gitignore`로 제외한다.
  → 커밋이 한 곳에서만 일어나 인지 부하가 낮고, 제출 repo에 학습 노트·구술 답변이 새지 않는다.
- **연결 고리는 URL이다.** repo URL·배포 URL·브랜치는 `MISSION_SPEC.md` 머리표와 `INDEX.md`에 기록한다.
  이 두 곳이 모노레포와 제출물을 잇는 유일한 접점이다.
- 제출 브랜치는 별도 지시가 없으면 `main`.
- 팀 과제(Git 협업 등)는 조직 repo를 같은 방식으로 clone해 둔다.
- 커밋 메시지는 **한국어**, Conventional Commits 형식.
  타입/스코프만 영문: `feat(b1-1): monitor.sh 리소스 수집 및 임계값 경고 구현`

---

## 2. 미션 파이프라인 (6단계)

과제 내용이 전달되면 항상 이 순서로 진행한다. 단계를 건너뛰지 않는다.

| 단계 | 이름 | 입력 | 산출물 | 완료 조건 |
|---|---|---|---|---|
| 1 | **접수** Intake | 과제 원문 | `source/` | 원문이 손실 없이 텍스트로 확보됨 |
| 2 | **분석** Analyze | `source/` | R/E/C/V ID 목록 | 요구사항이 원자 단위로 쪼개짐 |
| 3 | **명세** Spec | 분석 결과 | `MISSION_SPEC.md` | 추적 매트릭스가 빈칸 없이 채워짐 |
| 4 | **구현** Build | 명세 | `repo/` 코드 | 모든 R-ID에 코드 앵커가 존재 |
| 5 | **검증** Verify | 코드 | `evidence/`, `CHECKLIST.md` | 모든 V-ID가 증거와 함께 PASS |
| 6 | **방어** Defend | 전체 | `NOTE.md` 완성 | 모든 E-ID에 내 말로 된 답이 있음 |

**1단계 접수**: 과제 원문(PDF/이미지/텍스트)을 `source/`에 보관한다. 원문은 절대 편집하지 않는다.
요약본이 필요하면 `source/extracted.md`로 따로 만든다.

**2단계 분석**: 원문을 아래 ID 체계로 분해한다. 이게 이후 모든 추적의 기준이 된다.

**3단계 명세**: `MISSION_SPEC.md`를 채운다. 여기서 "무엇을 만들지"가 확정된다.
구현 중 명세를 벗어나야 할 일이 생기면 코드가 아니라 **명세를 먼저 고친다.**

**4단계 구현**: §4 주석 규약을 지키며 작성한다.

**5단계 검증**: 체크리스트를 기계적으로 실행하고 증거를 남긴다.
"될 것이다"가 아니라 "실행했고 이 출력을 얻었다"만 PASS로 인정한다.

**6단계 방어**: 평가문항 항목 2~4는 구술이다. `NOTE.md`의 Q&A를 **보지 않고** 말할 수 있어야 끝난다.

> **평가지를 아직 못 받은 과제**: `E-ID`는 과제 원문의 "과제 목표(스스로 설명할 수 있어야 한다)"와
> `references/eval-criteria.md`의 질문 어휘 패턴으로 **예측해서 채운다.** 예측임을 명시하고,
> 실제 평가지를 받으면 교체한다. 비워두지 않는다 — 구술 대비는 평가지가 와서 시작하면 늦다.

---

## 3. ID 체계 — 구현 / 개념 / 평가의 분리와 연결

사용자 요구인 "구현 / 공부할 부분 / 개념"의 3분할을 ID로 고정한다.
분리해서 쓰되, ID로 서로를 가리키게 해서 **끊기지 않게** 만든다.

| 접두사 | 뜻 | 어디에 사는가 | 예 |
|---|---|---|---|
| `R-n` | **기능 요구사항** (구현해야 할 것) | `MISSION_SPEC.md` → 코드 주석 | `R-12` 모바일에서 햄버거 버튼 클릭 시 메뉴가 토글된다 |
| `C-n` | **개념** (알아야 할 것) | `NOTE.md` 개념 카드 | `C-2` 최소 권한 원칙과 ACL |
| `E-a.b` | **평가문항** (질문받을 것) | `NOTE.md` 구술 Q&A | `E-3.1` Flexbox와 Grid를 각각 어디에 왜 썼는가 |
| `V-n` | **검증 절차** (증명할 것) | `CHECKLIST.md` | `V-4` 다크 모드 토글 후 새로고침 → 설정 유지 확인 |

### 추적 매트릭스 (MISSION_SPEC.md에 필수)

모든 `R`은 최소 하나의 `V`를 가져야 하고, 모든 `E`는 최소 하나의 `C`를 가져야 한다.
빈칸이 있으면 그 미션은 아직 착수 준비가 안 된 것이다.

| R-ID | 요구사항 | 구현 위치 | V-ID | 관련 C-ID | 관련 E-ID |
|---|---|---|---|---|---|
| R-1 | … | `src/x.sh:12-40` | V-1 | C-1, C-3 | E-1.1, E-2.2 |

---

## 4. 주석 규약 — "무슨 작업인지"와 "왜 이렇게 썼는지"의 분리

> **이 절이 이 명세의 핵심이다.**
> AI가 작성한 코드든 직접 작성한 코드든, 읽는 사람이 *작업 내용*과 *설계 근거*를
> 구분해서 파악할 수 있어야 한다. 구술 평가는 정확히 이 두 가지를 묻는다.

### 3계층 구조

**① 파일 헤더** — 이 파일이 무슨 책임을 지는가

```bash
# ==============================================================================
# monitor.sh — 시스템 상태 수집 및 로깅
#
# 담당 요구사항 : R-4(헬스체크), R-5(자원 수집), R-6(임계값 경고), R-7(로그 기록)
# 실행 주체     : agent-admin (cron), 소유자 agent-dev, 권한 750
# 의존          : ps, ss, awk — 외부 패키지 설치 불필요 (제약사항: Bash 전용)
# 실패 정책     : 헬스체크 실패 → exit 1 / 임계값 초과 → 경고만 출력 후 계속
# ==============================================================================
```

**② 블록·함수 주석** — WHAT과 WHY를 반드시 분리해 적는다

```bash
# [R-5][C-3] CPU 사용률 수집
# WHAT: /proc/stat 2회 샘플링으로 delta를 구해 순간 사용률(%)을 계산한다.
# WHY : top -bn1은 첫 샘플이 부팅 이후 누적 평균이라 실시간 부하를 못 잡는다.
#       ps의 %CPU도 같은 이유로 부정확하다. 2회 샘플링이 추가 의존성 없이
#       정확한 유일한 방법이라 선택했다.
```

- `WHAT:` — **무슨 작업이 처리되는가.** 코드를 안 읽어도 동작을 알 수 있게.
- `WHY:` — **왜 이 방식인가.** 반드시 *기각한 대안*을 함께 적는다. 대안이 없으면 WHY가 아니다.
- `[R-n][C-n]` 앵커 — 명세·노트와의 연결선. 앵커 없는 핵심 로직은 추적 불가로 간주한다.

**③ 인라인 주석** — 자명하지 않은 한 줄에만, 한 줄로

```bash
awk '{print $2}' # 2번째 필드 = PID (ps -ef 기준)
```

### 금지

- 코드를 그대로 읽는 주석 — `i++ # i를 1 증가` (정보량 0)
- 주석 처리된 죽은 코드 — 지운다. git이 기억한다.
- `WHY` 없는 비자명한 선택 — 왜 하필 그 명령어/자료구조/포맷인지 안 적혀 있으면 구술에서 막힌다.
- 영어 서술 주석 — 구술 평가가 한국어다. **주석은 한국어**로 쓰되 명령어·식별자·로그 문자열은 원문 유지.

### AI 생성 코드에 대한 추가 규칙 (설명 부채 금지)

AI가 생성한 코드는 다음 두 가지가 **동시에** 갖춰지기 전까지 `DONE`이 아니다.

1. 해당 블록에 `WHAT` / `WHY` 주석과 `[R-n][C-n]` 앵커가 있다.
2. `NOTE.md`의 **§3 코드 해설**에 그 파일의 처리 흐름이 내 말로 다시 쓰여 있다.

이유: 평가 항목 4는 "본인의 판단 흐름을 논리적으로 제시할 수 있는가"를 묻는다.
설명이 붙지 않은 생성 코드는 그 자리에서 `재구현 불가` 판정으로 직결된다.

---

## 5. 학습 산출물 — 미션별 단일 노트

개념·학습·구술 대비는 **미션 폴더 안의 `NOTE.md` 하나**로 합친다. (별도 개념 라이브러리를 두지 않는다.)

- 장점: 미션 문맥과 개념이 붙어 있어 복습이 빠르다. 파일 탐색 비용이 0이다.
- 감수할 비용: 개념이 미션 간 중복될 수 있다.
  → 중복이 3회 이상 쌓인 개념은 `references/`로 승격하고 노트에서는 링크만 남긴다. (그때 가서 한다. 미리 안 한다.)

`NOTE.md` 구성은 `templates/NOTE.md` 참조. 6개 절 고정:
미션 요약 / 개념 카드(C) / 코드 해설 / 구술 Q&A(E) / 막힌 지점 로그 / 다시 한다면.

---

## 6. 완료 정의 (Definition of Done)

미션은 아래 6개가 모두 참일 때만 `DONE`으로 `INDEX.md`에 기록한다.

1. `MISSION_SPEC.md`의 추적 매트릭스에 빈칸이 없다.
2. 모든 `R-ID`가 코드 앵커로 추적된다.
3. 모든 `V-ID`가 `evidence/`의 실제 출력과 함께 PASS다.
4. 모든 `E-ID`에 대해 노트를 보지 않고 답할 수 있다.
5. 제출 repo URL과 브랜치가 `INDEX.md`에 기록되어 있다.
6. 보너스 과제는 수행/미수행 여부가 명시되어 있다. (미수행도 기록한다.)

---

## 7. 작업 규칙

- **원문 우선**: 요구사항 판단이 갈리면 `source/` 원문이 이긴다. 추측으로 채우지 않는다.
- **증거 우선**: 검증은 실행 결과로만 한다. `evidence/`에 타임스탬프가 찍힌 출력을 남긴다.
- **제약 준수**: 미션별 언어/도구 제약(예: "Bash로만 작성", "바이너리 리버싱 금지")은 절대 조건이다.
- **범위 고정**: 명세에 없는 기능을 임의로 추가하지 않는다. 필요하면 명세를 먼저 고친다.
- **한 커밋 한 관심사**: 요구사항 단위로 커밋한다. 커밋 본문에 `R-ID`를 적는다.
